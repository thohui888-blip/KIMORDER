# KIM ORDER 项目说明

## 项目概述
KIM FROZEN FOOD SDN BHD内部订单管理工具，托管在GitHub Pages。
前端：HTML/CSS/JS单文件，仓库：thohui888-blip/KIMORDER
后端：Google Apps Script独立项目（KIM ORDER），接收POST请求写入Google Sheets。
每天需要更新Sheets ID。

## 车次分组规则
订单按delivery车次分组：A、B、C、D、E、F、G、JW、BP、其他
- 车次字母在客户名称或备注中识别
- NEW = 新客户，需特别标注
- 加货 = 补单追加，合并到同一客户
- 取消 = 删除该项
- 分单 = 同一客户拆成多张单

## 已知边缘案例

### 一、车次字母识别（detectVehicle）
**已支持的格式**
- 括号格式：(A)、（A）、(A车)、（A车）、[A]、【A】
- X车格式：Restaurant Chia ping D车（字母+车在行中任意位置）
- 行尾字母：好滋味 F、廷香哥罗面 D（前面有空格）
- 紧贴字母：皇家E、富力B（字母紧贴店名无空格）
- 小写字母：张记(pulai) b（大小写不敏感）
- 字母+加货：8sam A 加、星星G加、蓝天菜饭 B加货、天空 C加、Loong Hua D加（加/加货/加单/+ 都支持，紧贴或有空格都行）
- 字母+括号备注：新加坡beletime E(收Cash)（字母在中间，后接括号备注）
- 字母+日期：八星菜饭 B 18/04（字母后接日期备注）

**必须排除的误判（已修复）**
- 单位结尾不算车次：正鸡腿 4kg、肉片 10kg、鸡骨 20kg、正鸡腿1ctn、龙虿鱼头 20k（kg/ctn/k 等结尾的字母不误判为车次）。规则用 (?<![0-9a-zA-Z]) 排除字母前是数字或字母的情况
- 括号字母+数量=货物不是店名：咸蛋黄(A) 30包、夹心肉（A）1箱、鸡蛋(B) 两箱、鸡胸(A) 一箱、咸蛋黄(A) 三十包（括号后跟数量的是货物，要 fall through）
- 支持中文数字量词：一、二、两、三…十、百、半 + 字符 = 货物
- 日期格式不算数量（仍是店名）：887～肉骨茶(B)星期6、榀香楼(D)星期6、31 Food Court 肉骨茶(B)星期6（排除 星期X、X/X、X月、X号、X日）

**已知无法处理（需人工注意）**
- 字母后跟不可预测的文字备注：Wan xi heng B 转帐（B在中间，"转帐"在行尾，无括号，识别不到——备注词无法穷举，复制时自己留意）
- 改车次订单：Cash 金城-云吞面 A 下一行 改去 有饭菜馆 @面 G（A和G都会被识别，app会拆成两家店——复制时手动删掉旧字母）
- 没有车次字母的店名夹在订单中间且无空行：食家庄Gaya、泰国餐（机器无法区分"无字母店名"和"货物行"，会被并入上一家店的货物。订单不会丢失，进了Sheet就好）

### 二、特殊订单格式
**NEW 新顾客**
- 格式：New Lim、New: Toh、New：名字、New+名字、*New kim*（支持星号包围）
- 处理：New 名字 单独成行时，记住名字，等下一个有车次的店名出现，合并成 New（名字）店名，标蓝色
- 找不到车次的NEW：归入JW组，标注"（无车次，请确认）"，不丢失
- 提取名字时去掉星号和 New 前缀

**加货（add）**
- 触发词：行尾"加"、"加货"、"加单"
- 标橙色
- 注意：纯文字规则只标记，不做实际合并（用户要求不自动合并，避免出错）

**分单（split）**
- 触发词："分单"
- 标橙红色
- 保留为独立两条，不合并

**取消（cancel）**
- 触发词："取消"、"cancel"
- 标红色

**改名/换名**
- 触发词："改名"、"换名"、"更名"
- 处理：保存当前店，等下一个有车次的店名，把改名行作为该店第一条备注

### 三、JW/其他组（显示名："JW/BP+自来/其他"）
- 触发词：JW、walk in/walkin、自取、自己来
- 已移除"其他"：因为 A鸭 2.6- 5只 如果没有其他 会被误判切断订单。Sheet里栏位名叫"其他"，但app不检测"其他"二字（没人会用"其他"写路线）
- 识别不到车次的店名 → 自动归入JW，标注"（无车次）"，确保不丢单

### 四、待讨论未实现（司机简称）
司机简称对应车次（尚未实现，有冲突待确认）：
A=Micheal、B=Lim、C=Wei、D=Hong、E=Boy、F=CK、G=Hui、JW=JW

冲突点：
- New Lim 的 Lim 是顾客名也是B车司机
- 店名含司机名：Chuan Wei Xuan(D)含Wei、Liuzhouboy E含boy、Wei Rong（D车）含Wei（需字母规则优先，司机名须为独立词）
- 寄 字可能出现在"寄放/寄过去"等货物备注（需限定独立字或行尾）
- 可能有店名叫 Hong Kee 等含司机名但无车次字母（会误判）

### 五、技术架构注意事项
- 写入方式：app（GitHub Pages）→ POST → Apps Script（独立项目 KIM ORDER）→ 写入 Google Sheets
- 不用 API Key / OAuth：API Key 只能读，OAuth Desktop 不支持本地——最终用 Apps Script doPost
- GitHub 上传必须用 "Upload files"：复制粘贴进编辑器会因内容过长被截断，导致 JS 解析失败、所有按钮失效
- 列映射：A=B列、B=E列、C=H列、D=K列、E=N列、F=Q列、G=T列、JW=W列，主Sheet名"abcde"，起始行2
- 每天换Sheet：复制模板勾选"Share with same people"，把新Sheet链接贴进app（自动提取ID），Apps Script URL不变
- JS函数作用域坑：内部函数引用的变量（如 pendingNew）必须在函数定义前声明，否则整个script崩溃、按钮失效
- no-cors模式：POST用no-cors，返回opaque无法读结果，默认不报错就算成功

## 重要提醒
- 改动前先给我看diff再提交
- 不要动Google Sheets写入的字段格式